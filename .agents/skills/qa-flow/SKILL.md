---
name: qa-flow
description: Use when the user asks to QA an environment, reproduce one of the known bugs (BUG-1 through BUG-11), validate a feature end-to-end, run the smoke suite, or set up the browser-MCP dev-bypass flow against dev.tinboker.com.
---

Read `docs/workflows/qa-flow.md` and follow the procedure. The bug-reproduction matrix and the dev-bypass token URL pattern are in that doc; the full per-endpoint/per-page QA suite lives in `docs/agents/qa-tester.md` and the bug catalog in `docs/qa-report-2026-05-09.md`.
